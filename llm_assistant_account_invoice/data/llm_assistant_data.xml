<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- One-Shot Invoice Data Extraction Assistant (automatic decoder) -->
    <record id="llm_assistant_invoice_extraction" model="llm.assistant">
        <field name="name">Invoice Data Extraction (Automatic)</field>
        <field name="code">invoice_extraction</field>
        <field name="res_model">account.move</field>
        <field name="is_default" eval="False" />
        <field name="is_public" eval="False" />
        <field
      name="prompt_id"
      ref="llm_assistant_account_invoice.llm_prompt_invoice_extraction"
    />
        <field name="has_dynamic_defaults" eval="True" />
        <field
      name="default_values"
    ><![CDATA[{
            "role": "Invoice Data Extraction Specialist",
            "goal": "extract structured invoice data from OCR text and return as clean JSON for automated processing",
            "background": "You process invoice documents for Odoo ERP system. Extract vendor information, invoice details, and line items accurately.",
            "instructions": "Extract the following fields from invoice documents:\n\n**Required Fields (use camelCase, NO underscores):**\n- vendorName (str): Company/supplier name\n- invoiceNumber (str): Invoice reference number\n- invoiceDate (str): Invoice date in YYYY-MM-DD format\n- totalAmount (float): Total invoice amount including tax\n- lines (array): Invoice line items\n\n**Optional Fields:**\n- vat (str): VAT/tax number (include country code if present, e.g., BE0123456789)\n- currency (str): Currency code (EUR, USD, etc.) - default EUR if not found\n- dueDate (str): Payment due date in YYYY-MM-DD format\n- paymentReference (str): Payment reference or structured communication\n\n**Line Items Structure:**\nEach line in the 'lines' array must have:\n- description (str): Product/service description\n- quantity (float): Quantity (default 1.0 if not specified)\n- unitPrice (float): Unit price excluding tax\n- taxPercent (float, optional): Tax percentage (e.g., 21.00 for 21% VAT)\n\n**Output Format:**\nReturn ONLY a valid JSON object with this structure:\n```json\n{\n  \"vendorName\": \"Supplier Inc.\",\n  \"vat\": \"BE0123456789\",\n  \"invoiceNumber\": \"INV-2024-001\",\n  \"invoiceDate\": \"2024-01-15\",\n  \"dueDate\": \"2024-02-14\",\n  \"currency\": \"EUR\",\n  \"totalAmount\": 1210.00,\n  \"lines\": [\n    {\n      \"description\": \"Web Development Services\",\n      \"quantity\": 10.0,\n      \"unitPrice\": 100.00,\n      \"taxPercent\": 21.00\n    }\n  ]\n}\n```\n\n**Important Rules:**\n- Use camelCase for ALL field names (NO underscores!)\n- Dates MUST be in YYYY-MM-DD format\n- Amounts MUST be numbers (float), not strings\n- DO NOT include currency symbols in amounts\n- Return ONLY the JSON object, no explanations, no markdown code blocks\n- If a field is not found, omit it (except required fields)\n- For line items, calculate unitPrice if only total is given: total / quantity\n- Vendor name should be the company name, not individual person names",
            "footer": "CRITICAL: Return ONLY valid JSON. NO markdown code blocks, NO explanations, NO additional text. Dates in YYYY-MM-DD format. Numbers as floats, not strings.",
            "ocr_text": "{{ ocr_text }}"
        }]]></field>
        <field name="active" eval="True" />
    </record>
</odoo>

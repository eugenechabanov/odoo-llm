<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View -->
    <record id="llm_crew_task_view_form" model="ir.ui.view">
        <field name="name">llm.crew.task.form</field>
        <field name="model">llm.crew.task</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Task Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="task_id"/>
                            <field name="agent_id"/>
                            <field name="output_format"/>
                            <field name="async_execution"/>
                        </group>
                        <group>
                            <field name="execution_time" widget="float_time"/>
                            <field name="tools"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" placeholder="Task description for the AI agent"/>
                        </page>
                        <page string="Expected Output">
                            <field name="expected_output" placeholder="Description of the expected output"/>
                        </page>
                        <page string="Context">
                            <field name="context" placeholder="Additional context for the task"/>
                        </page>
                        <page string="Result" attrs="{'invisible': [('state', 'not in', ['completed', 'failed'])]}">
                            <group>
                                <field name="result" readonly="1"/>
                                <field name="error" readonly="1"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Tree View -->
    <record id="llm_crew_task_view_tree" model="ir.ui.view">
        <field name="name">llm.crew.task.tree</field>
        <field name="model">llm.crew.task</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="task_id"/>
                <field name="agent_id"/>
                <field name="state"/>
                <field name="execution_time" widget="float_time"/>
            </tree>
        </field>
    </record>

    <!-- Search View -->
    <record id="llm_crew_task_view_search" model="ir.ui.view">
        <field name="name">llm.crew.task.search</field>
        <field name="model">llm.crew.task</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="task_id"/>
                <field name="agent_id"/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="In Progress" name="in_progress" domain="[('state', '=', 'in_progress')]"/>
                <filter string="Completed" name="completed" domain="[('state', '=', 'completed')]"/>
                <filter string="Failed" name="failed" domain="[('state', '=', 'failed')]"/>
                <group expand="0" string="Group By">
                    <filter string="State" name="group_by_state" context="{'group_by': 'state'}"/>
                    <filter string="Agent" name="group_by_agent" context="{'group_by': 'agent_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_llm_crew_task" model="ir.actions.act_window">
        <field name="name">AI Tasks</field>
        <field name="res_model">llm.crew.task</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_draft': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first AI task!
            </p>
        </field>
    </record>

    <!-- Menu Item -->
    <menuitem id="menu_llm_crew_task"
              name="AI Tasks"
              parent="menu_llm_crew_root"
              action="action_llm_crew_task"
              sequence="20"/>
</odoo>
